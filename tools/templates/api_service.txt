import 'package:dio/dio.dart';

import '../../../core/constants/api_constants.dart';
import '../../../data/network/api_result.dart';
import '../../../data/network/base_api_service.dart';
// import '../domain/models/{name}.dart'; // Uncomment when model is ready

class {PascalName}ApiService extends BaseApiService {
  {PascalName}ApiService({required Dio dio}) : _dio = dio;

  final Dio _dio;

  // Example network call
  /*
  Future<ApiResult<List<{PascalName}>>> getItems() {
    return request<List<{PascalName}>>(
      call: _dio.get('/{name}s'),
      mapper: (data) {
        final list = data as List<dynamic>;
        return list.map((json) => {PascalName}.fromJson(json as Map<String, dynamic>)).toList();
      },
    );
  }
  */
}
